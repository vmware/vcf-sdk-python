This directory contains basic samples for NSX APIs:

Running the CRUD samples

    $ python crud.py --server <NSX Server IP> --user <username> --password <password>

* Testbed Requirement:
   - 1 NSX Server
